name: Deterministic Checks

on:
  pull_request:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run deterministic checks
        run: ./.beryl/scripts/check.sh

      - name: Run agent doctor
        run: ./.beryl/agent/scripts/agent-doctor.sh
