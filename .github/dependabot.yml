version: 2
updates:
  # Keep SHA-pinned action digests current (Dependabot updates the pinned
  # commit and version comment together).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
